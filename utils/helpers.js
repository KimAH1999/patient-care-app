//helpers commented out for time being, this section can be used to create format for date and time.
//Often times date and time is an important section for legal reasons and proper work flow for appts/check-in/check-out.
/*module.exports = {
    format_time: (date) => {
        return date.toLocaleTimeString();
    },
    format_date: (date) => {
        return `${new Date(date).getMonth() + 1}/${new Date(date).getDate()}/${new Date(date).getFullYear() + 5
            }`;
    },
};*/
