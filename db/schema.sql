DROP DATABASE IF EXISTS patient_db;
CREATE DATABASE patient_db;

USE patient_db;
/*Future plans would be by adding more data in patient_db database created*/
/*CREATE TABLE chartList (
    id, INT NOT NULL,
    first_name VARCHAR(30) NOT NULL,
    last_name VARCHAR(30) NOT NULL,
    value_card_member BOOLEAN NOT NULL
);*/